<section id="projects" class="section-alt">
  <div class="section-inner">
    <h2 class="section-heading">Projects</h2>
    <div class="projects-grid">
      <a href="https://github.com/shenri1/Beret" target="_blank" rel="noopener noreferrer" class="project-card reveal">
        <div class="project-title-row">
          <h3 class="project-title">Beret</h3>
          <svg class="project-icon" viewBox="0 0 24 24" width="16" height="16">
            <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
            <polyline points="15 3 21 3 21 9" />
            <line x1="10" y1="14" x2="21" y2="3" />
          </svg>
        </div>
        <p class="project-desc">
          Project hosted on GitHub.
        </p>
        <div class="project-tags">
          <span class="project-tag">GitHub</span>
        </div>
      </a>

      <a href="https://github.com/shenri1/ConsumidorGOV-Scraper" target="_blank" rel="noopener noreferrer" class="project-card reveal">
        <div class="project-title-row">
          <h3 class="project-title">ConsumidorGOV Scraper</h3>
          <svg class="project-icon" viewBox="0 0 24 24" width="16" height="16">
            <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
            <polyline points="15 3 21 3 21 9" />
            <line x1="10" y1="14" x2="21" y2="3" />
          </svg>
        </div>
        <p class="project-desc">
          Scraper for ConsumidorGOV platform hosted on GitHub.
        </p>
        <div class="project-tags">
          <span class="project-tag">GitHub</span>
        </div>
      </a>
    </div>
  </div>
</section>

<style>
  .section-alt {
    background-color: color-mix(in srgb, var(--bg-secondary), transparent 95%);
    border-top: 1px solid color-mix(in srgb, var(--text-primary), transparent 95%);
    border-bottom: 1px solid color-mix(in srgb, var(--text-primary), transparent 95%);
  }

  .section-inner {
    max-width: 80rem;
    margin: 0 auto;
    padding: 8rem 1.5rem;
  }

  .section-heading {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    text-transform: uppercase;
    letter-spacing: 0.4em;
    color: var(--text-secondary);
    border-left: 2px solid var(--accent);
    padding-left: 1rem;
    margin-bottom: 4rem;
  }

  .projects-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    max-width: 52rem;
    margin: 0 auto;
  }
  @media (min-width: 768px) {
    .projects-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .project-card {
    display: flex;
    flex-direction: column;
    padding: 2rem;
    border: 1px solid color-mix(in srgb, var(--text-primary), transparent 90%);
    border-radius: 0.25rem;
    background-color: color-mix(in srgb, var(--bg-secondary), transparent 95%);
    transition:
      border-color 0.2s,
      background-color 0.2s;
  }
  .project-card:hover {
    border-color: color-mix(in srgb, var(--accent), transparent 60%);
    background-color: color-mix(in srgb, var(--bg-secondary), transparent 90%);
  }

  .project-title-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }

  .project-title {
    font-size: 1.25rem;
    font-weight: 700;
    transition: color 0.2s;
  }
  .project-card:hover .project-title {
    color: var(--accent);
  }

  .project-icon {
    color: var(--text-secondary);
    opacity: 0.3;
    transition: opacity 0.2s;
  }
  .project-card:hover .project-icon {
    opacity: 1;
  }

  .project-desc {
    color: var(--text-secondary);
    font-size: 0.875rem;
    line-height: 1.75;
    font-weight: 300;
    opacity: 0.8;
    margin-bottom: 1.5rem;
  }

  .project-tags {
    display: flex;
    gap: 1rem;
    margin-top: auto;
  }

  .project-tag {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    color: var(--text-secondary);
    letter-spacing: 0.2em;
    text-transform: uppercase;
    opacity: 0.4;
    font-weight: 500;
  }
</style>
