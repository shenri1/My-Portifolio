<section id="projects" class="section-alt">
  <div class="section-inner">
    <h2 class="section-heading" data-key="projects.title">
      Projects
    </h2>
    <div class="projects-grid">
      <div class="project-card reveal">
        <div class="project-img-wrap">
          <img
            src="https://picsum.photos/seed/tech1/400/250"
            alt="Nexus Dashboard"
            referrerpolicy="no-referrer"
          />
        </div>
        <div class="project-title-row">
          <h3 class="project-title">Nexus Dashboard</h3>
          <svg class="project-icon" viewBox="0 0 24 24" width="16" height="16">
            <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
            <polyline points="15 3 21 3 21 9" />
            <line x1="10" y1="14" x2="21" y2="3" />
          </svg>
        </div>
        <p class="project-desc">
          Real-time analytics engine for monitoring performance.
        </p>
        <div class="project-tags">
          <span class="project-tag">React</span>
          <span class="project-tag">D3.js</span>
        </div>
      </div>

      <div class="project-card reveal">
        <div class="project-img-wrap">
          <img
            src="https://picsum.photos/seed/tech3/400/250"
            alt="Circuit UI"
            referrerpolicy="no-referrer"
          />
        </div>
        <div class="project-title-row">
          <h3 class="project-title">Circuit UI</h3>
          <svg class="project-icon" viewBox="0 0 24 24" width="16" height="16">
            <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
            <polyline points="15 3 21 3 21 9" />
            <line x1="10" y1="14" x2="21" y2="3" />
          </svg>
        </div>
        <p class="project-desc">
          Modular UI library for technical interfaces.
        </p>
        <div class="project-tags">
          <span class="project-tag">TypeScript</span>
          <span class="project-tag">Tailwind</span>
        </div>
      </div>

      <div class="project-card reveal">
        <div class="project-img-wrap">
          <img
            src="https://picsum.photos/seed/tech2/400/250"
            alt="API Gateway"
            referrerpolicy="no-referrer"
          />
        </div>
        <div class="project-title-row">
          <h3 class="project-title">API Gateway</h3>
          <svg class="project-icon" viewBox="0 0 24 24" width="16" height="16">
            <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
            <polyline points="15 3 21 3 21 9" />
            <line x1="10" y1="14" x2="21" y2="3" />
          </svg>
        </div>
        <p class="project-desc">
          High-performance gateway with rate limiting.
        </p>
        <div class="project-tags">
          <span class="project-tag">Go</span>
          <span class="project-tag">Redis</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .section-alt {
    background-color: color-mix(in srgb, var(--bg-secondary), transparent 95%);
    border-top: 1px solid color-mix(in srgb, var(--text-primary), transparent 95%);
    border-bottom: 1px solid color-mix(in srgb, var(--text-primary), transparent 95%);
  }

  .section-inner {
    max-width: 80rem;
    margin: 0 auto;
    padding: 8rem 1.5rem;
  }

  .section-heading {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    text-transform: uppercase;
    letter-spacing: 0.4em;
    color: var(--text-secondary);
    border-left: 2px solid var(--accent);
    padding-left: 1rem;
    margin-bottom: 4rem;
  }

  .projects-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
  }
  @media (min-width: 768px) {
    .projects-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .project-card {
    display: flex;
    flex-direction: column;
  }

  .project-img-wrap {
    aspect-ratio: 16 / 10;
    margin-bottom: 2rem;
    overflow: hidden;
    border-radius: 0.25rem;
    border: 1px solid color-mix(in srgb, var(--text-primary), transparent 90%);
    flex-shrink: 0;
  }

  .project-img-wrap img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: grayscale(1);
    opacity: 0.6;
    transition:
      filter 0.7s,
      opacity 0.7s;
  }
  .project-card:hover .project-img-wrap img {
    filter: grayscale(0);
    opacity: 1;
  }

  .project-title-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }

  .project-title {
    font-size: 1.25rem;
    font-weight: 700;
    transition: color 0.2s;
  }
  .project-card:hover .project-title {
    color: var(--accent);
  }

  .project-icon {
    color: var(--text-secondary);
    opacity: 0.3;
    transition: opacity 0.2s;
  }
  .project-card:hover .project-icon {
    opacity: 1;
  }

  .project-desc {
    color: var(--text-secondary);
    font-size: 0.875rem;
    line-height: 1.75;
    font-weight: 300;
    opacity: 0.8;
    margin-bottom: 1.5rem;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .project-tags {
    display: flex;
    gap: 1rem;
    margin-top: auto;
  }

  .project-tag {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    color: var(--text-secondary);
    letter-spacing: 0.2em;
    text-transform: uppercase;
    opacity: 0.4;
    font-weight: 500;
  }
</style>
