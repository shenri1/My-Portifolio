<section id="contact">
  <div class="section-inner">
    <h2 class="contact-heading">Connect</h2>

    <div class="contact-grid">
      <div class="reveal">
        <p class="contact-label">Electronic Mail</p>
        <p class="contact-email">silas.particular05@gmail.com</p>
        <div class="social-links">
          <a href="https://github.com/shenri1" aria-label="GitHub">
            <svg viewBox="0 0 24 24">
              <path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22" />
            </svg>
          </a>
          <a href="https://www.linkedin.com/in/silas-cerqueira/" aria-label="LinkedIn">
            <svg viewBox="0 0 24 24">
              <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z" />
              <rect x="2" y="9" width="4" height="12" />
              <circle cx="4" cy="4" r="2" />
            </svg>
          </a>
        </div>
      </div>

      <div class="reveal">
        <p class="contact-label">Resume Archive</p>
        <a href="/Curriculum_Vitae___English.pdf" download class="resume-btn">
          <span>ENGLISH CV</span>
          <svg viewBox="0 0 24 24" width="14" height="14">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
            <polyline points="7 10 12 15 17 10"></polyline>
            <line x1="12" y1="15" x2="12" y2="3"></line>
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .section-inner {
    max-width: 80rem;
    margin: 0 auto;
    padding: 8rem 1.5rem;
  }

  .contact-heading {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    text-transform: uppercase;
    letter-spacing: 0.4em;
    color: var(--text-secondary);
    border-bottom: 1px solid color-mix(in srgb, var(--text-primary), transparent 90%);
    padding-bottom: 1rem;
    display: inline-block;
    margin-bottom: 4rem;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 6rem;
  }
  @media (min-width: 768px) {
    .contact-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
  @media (min-width: 1024px) {
    .contact-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .contact-label {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    color: var(--text-secondary);
    text-transform: uppercase;
    opacity: 0.5;
    letter-spacing: 0.2em;
    font-weight: 700;
    margin-bottom: 1.5rem;
  }

  .contact-email {
    font-size: 1.5rem;
    font-weight: 300;
    word-break: break-all;
    cursor: pointer;
    transition: color 0.2s;
  }
  .contact-email:hover {
    color: var(--accent);
  }

  .social-links {
    display: flex;
    gap: 2rem;
    padding-top: 1rem;
  }
  .social-links a {
    color: var(--text-secondary);
    transition: color 0.2s;
  }
  .social-links a:hover {
    color: var(--accent);
  }
  .social-links svg {
    width: 1.5rem;
    height: 1.5rem;
  }

  .resume-btn {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1rem 1.5rem;
    border: 1px solid color-mix(in srgb, var(--text-primary), transparent 90%);
    border-radius: 0.25rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-secondary);
    background-color: color-mix(in srgb, var(--bg-secondary), transparent 95%);
    transition:
      color 0.2s,
      border-color 0.2s;
    width: 100%;
    margin-bottom: 1rem;
  }
  .resume-btn:hover {
    color: var(--accent);
    border-color: color-mix(in srgb, var(--accent), transparent 60%);
  }
  .resume-btn svg {
    opacity: 0.4;
    transition: opacity 0.2s;
  }
  .resume-btn:hover svg {
    opacity: 1;
  }
</style>
