<section id="contact">
    <div class="section-inner">
        <div class="section-label">// 05. CONNECT</div>

        <div class="contact-wrap reveal">
            <div class="contact-card">
                <div class="contact-header">
                    <span class="contact-status">
                        <span class="status-dot-green"></span>
                        Available for opportunities
                    </span>
                </div>

                <div class="contact-actions">
                    <a
                        href="mailto:silas.particular05@gmail.com"
                        class="contact-action email"
                    >
                        <div class="action-icon">
                            <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2">
                                <rect x="2" y="5" width="20" height="14" rx="2"/>
                                <polyline points="2 5 12 13 22 5"/>
                            </svg>
                        </div>
                        <div class="action-body">
                            <span class="action-label">Email</span>
                            <span class="action-value">silas.particular05@gmail.com</span>
                        </div>
                    </a>

                    <a
                        href="https://github.com/shenri1"
                        target="_blank"
                        rel="noopener noreferrer"
                        class="contact-action"
                    >
                        <div class="action-icon">
                            <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/>
                            </svg>
                        </div>
                        <div class="action-body">
                            <span class="action-label">GitHub</span>
                            <span class="action-value">@shenri1</span>
                        </div>
                    </a>

                    <a
                        href="https://www.linkedin.com/in/silas-cerqueira/"
                        target="_blank"
                        rel="noopener noreferrer"
                        class="contact-action"
                    >
                        <div class="action-icon">
                            <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/>
                                <rect x="2" y="9" width="4" height="12"/>
                                <circle cx="4" cy="4" r="2"/>
                            </svg>
                        </div>
                        <div class="action-body">
                            <span class="action-label">LinkedIn</span>
                            <span class="action-value">Silas Cerqueira</span>
                        </div>
                    </a>

                    <a
                        href="/Curriculum_Vitae___English.pdf"
                        target="_blank"
                        rel="noopener"
                        class="contact-action cv"
                    >
                        <div class="action-icon">
                            <svg viewBox="0 0 24 24" width="22" height="22" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                                <polyline points="7 10 12 15 17 10"/>
                                <line x1="12" y1="15" x2="12" y2="3"/>
                            </svg>
                        </div>
                        <div class="action-body">
                            <span class="action-label">Resume</span>
                            <span class="action-value">Download English CV</span>
                        </div>
                    </a>
                </div>
            </div>

            <div class="contact-footer">
                <p>Prefer email? I usually reply within 24 hours.</p>
            </div>
        </div>
    </div>
</section>

<style>
    .section-inner {
        max-width: 44rem;
        margin: 0 auto;
        padding: 4rem var(--space-lg);
    }

    .contact-wrap {
        display: flex;
        flex-direction: column;
        gap: var(--space-xl);
    }

    .contact-card {
        border: 1px solid var(--border);
        border-radius: var(--radius-xl);
        background-color: var(--bg-secondary);
        overflow: hidden;
    }

    .contact-header {
        padding: var(--space-md) var(--space-xl);
        background-color: var(--bg-accent);
        border-bottom: 1px solid var(--border);
        display: flex;
        align-items: center;
        gap: var(--space-sm);
    }

    .contact-status {
        font-family: var(--font-mono);
        font-size: 0.75rem;
        font-weight: 500;
        color: var(--text-secondary);
        display: inline-flex;
        align-items: center;
        gap: var(--space-sm);
    }

    .status-dot-green {
        width: 0.5rem;
        height: 0.5rem;
        border-radius: var(--radius-full);
        background-color: var(--accent-green);
        box-shadow: 0 0 8px var(--accent-green);
    }

    .contact-actions {
        display: flex;
        flex-direction: column;
    }

    .contact-action {
        display: flex;
        align-items: center;
        gap: var(--space-lg);
        padding: var(--space-lg) var(--space-xl);
        border-bottom: 1px solid var(--border);
        transition: background-color 0.2s, color 0.2s;
        color: inherit;
    }
    .contact-action:last-child {
        border-bottom: none;
    }
    .contact-action:hover {
        background-color: color-mix(in srgb, var(--bg-accent), transparent 60%);
    }
    .contact-action:hover .action-label {
        color: var(--accent);
    }
    .contact-action:hover .action-icon {
        color: var(--accent);
        opacity: 1;
    }

    .action-icon {
        width: 2.5rem;
        height: 2.5rem;
        border-radius: var(--radius-full);
        background-color: var(--bg-accent);
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--text-secondary);
        opacity: 0.7;
        flex-shrink: 0;
        transition: color 0.2s, opacity 0.2s;
    }

    .action-body {
        display: flex;
        flex-direction: column;
        gap: 0.125rem;
    }

    .action-label {
        font-family: var(--font-mono);
        font-size: 0.6875rem;
        font-weight: 500;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--text-muted);
        transition: color 0.2s;
    }

    .action-value {
        font-family: var(--font-sans);
        font-size: 1rem;
        font-weight: 500;
        color: var(--text-primary);
    }

    .contact-footer {
        text-align: center;
        color: var(--text-muted);
        font-family: var(--font-mono);
        font-size: 0.75rem;
    }
</style>
