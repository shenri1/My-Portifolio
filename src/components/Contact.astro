<section id="contact">
    <div class="section-inner">
        <div class="section-label">// 05. CONNECT</div>

        <div class="contact-grid">
            <div class="reveal">
                <p class="contact-label">Electronic Mail</p>
                <a
                    href="mailto:silas.particular05@gmail.com"
                    class="contact-email">silas.particular05@gmail.com</a
                >
                <div class="social-links">
                    <a href="https://github.com/shenri1" aria-label="GitHub">
                        <svg viewBox="0 0 24 24">
                            <path
                                d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"
                            ></path>
                        </svg>
                    </a>
                    <a
                        href="https://www.linkedin.com/in/silas-cerqueira/"
                        aria-label="LinkedIn"
                    >
                        <svg viewBox="0 0 24 24">
                            <path
                                d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"
                            ></path>
                            <rect x="2" y="9" width="4" height="12"></rect>
                            <circle cx="4" cy="4" r="2"></circle>
                        </svg>
                    </a>
                </div>
            </div>

            <div class="reveal">
                <p class="contact-label">Resume Archive</p>
                <a
                    href="/Curriculum_Vitae___English.pdf"
                    target="_blank"
                    rel="noopener"
                    class="resume-btn"
                >
                    <span>ENGLISH CV</span>
                    <svg viewBox="0 0 24 24" width="14" height="14">
                        <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"
                        ></path>
                        <polyline points="7 10 12 15 17 10"></polyline>
                        <line x1="12" y1="15" x2="12" y2="3"></line>
                    </svg>
                </a>
            </div>
        </div>
    </div>
</section>

<style>
    .section-inner {
        max-width: 75rem;
        margin: 0 auto;
        padding: 4rem var(--space-lg);
    }

    .contact-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: var(--space-2xl);
    }
    @media (min-width: 768px) {
        .contact-grid {
            grid-template-columns: 1fr 1fr;
        }
    }

    .contact-label {
        font-family: var(--font-mono);
        font-size: 0.6875rem;
        font-weight: 500;
        color: var(--text-muted);
        text-transform: uppercase;
        letter-spacing: 0.08em;
        margin-bottom: var(--space-md);
    }

    .contact-email {
        display: inline-block;
        font-family: var(--font-sans);
        font-size: 1.5rem;
        font-weight: 400;
        word-break: break-all;
        cursor: pointer;
        transition: color 0.2s;
        margin-bottom: var(--space-lg);
        text-decoration: none;
        color: var(--text-primary);
    }
    .contact-email:hover {
        color: var(--accent);
    }

    .social-links {
        display: flex;
        gap: var(--space-xl);
    }
    .social-links a {
        color: var(--text-secondary);
        transition: color 0.2s;
    }
    .social-links a:hover {
        color: var(--accent);
    }
    .social-links svg {
        width: 1.5rem;
        height: 1.5rem;
    }

    .resume-btn {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: var(--space-md);
        padding: var(--space-md) var(--space-lg);
        border: 1px solid var(--border);
        border-radius: var(--radius);
        font-family: var(--font-mono);
        font-size: 0.75rem;
        font-weight: 500;
        letter-spacing: 0.05em;
        color: var(--text-secondary);
        background-color: var(--bg-secondary);
        transition:
            color 0.2s,
            border-color 0.2s,
            box-shadow 0.3s;
        width: 100%;
        margin-bottom: var(--space-md);
    }
    .resume-btn:hover {
        color: var(--accent);
        border-color: color-mix(in srgb, var(--accent), transparent 50%);
        box-shadow: 0 0 24px var(--accent-glow);
    }
    .resume-btn svg {
        opacity: 0.4;
        flex-shrink: 0;
        transition: opacity 0.2s;
    }
    .resume-btn:hover svg {
        opacity: 1;
    }
</style>
