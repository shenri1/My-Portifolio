<section id="hero">
  <div class="hero-inner">
    <div class="hero-avatar-wrap">
      <div class="hero-avatar-ring">
        <img
          src="/1772192364889.jpg"
          alt="Silas Cerqueira"
          referrerpolicy="no-referrer"
        />
      </div>
      <div class="status-dot"></div>
    </div>

    <div class="hero-text">
      <div class="hero-status">
        <span class="slash">//</span>
        <span>Active System</span>
      </div>
      <h1 class="hero-name">
        Silas <br /><span class="hl">Cerqueira</span>
      </h1>
      <h2 class="hero-role">Computer Engineering</h2>
      <p class="hero-sub">
        Computer Engineering student focused on creating scalable software solutions and high-performance digital experiences.
      </p>
    </div>
  </div>
  <div class="hero-glow"></div>
</section>

<style>
  #hero {
    position: relative;
    min-height: 75vh;
    display: flex;
    align-items: center;
    padding: 6rem var(--space-lg) 4rem;
    overflow: hidden;
  }

  .hero-inner {
    max-width: 75rem;
    margin: 0 auto;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-3xl);
    animation: fadeUp 0.6s ease both;
  }

  @media (min-width: 768px) {
    .hero-inner {
      flex-direction: row;
      gap: 6rem;
      align-items: center;
    }
  }

  .hero-avatar-wrap {
    flex-shrink: 0;
    position: relative;
    animation: fadeScale 0.6s ease both;
  }

  .hero-avatar-ring {
    width: 14rem;
    height: 14rem;
    border-radius: var(--radius-full);
    border: 2px solid color-mix(in srgb, var(--accent), transparent 80%);
    padding: 0.5rem;
    overflow: hidden;
    background-color: color-mix(in srgb, var(--bg-secondary), transparent 90%);
  }

  @media (min-width: 768px) {
    .hero-avatar-ring {
      width: 18rem;
      height: 18rem;
    }
  }

  .hero-avatar-ring img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: var(--radius-full);
  }

  .status-dot {
    position: absolute;
    top: 1rem;
    right: 1rem;
    width: 1.5rem;
    height: 1.5rem;
    background-color: var(--accent-green);
    border-radius: var(--radius-full);
    border: 4px solid var(--bg-base);
    box-shadow: 0 0 12px var(--accent-green);
  }

  .hero-text {
    flex: 1;
    text-align: center;
    animation: fadeRight 0.6s 0.2s ease both;
  }
  @media (min-width: 768px) {
    .hero-text {
      text-align: left;
    }
  }

  .hero-status {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 500;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    opacity: 0.7;
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    justify-content: center;
    margin-bottom: var(--space-lg);
  }
  @media (min-width: 768px) {
    .hero-status {
      justify-content: flex-start;
    }
  }
  .hero-status .slash {
    color: var(--accent);
  }

  .hero-name {
    font-family: var(--font-sans);
    font-size: clamp(3rem, 10vw, 4.5rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.04em;
    margin-bottom: var(--space-lg);
  }
  .hero-name .hl {
    color: var(--accent);
  }

  .hero-role {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--space-xl);
  }
  @media (min-width: 768px) {
    .hero-role {
      font-size: 1.5rem;
    }
  }

  .hero-sub {
    color: var(--text-secondary);
    font-size: 1.125rem;
    line-height: 1.6;
    font-weight: 400;
    max-width: 36rem;
    margin: 0 auto;
  }
  @media (min-width: 768px) {
    .hero-sub {
      margin: 0;
    }
  }

  .hero-glow {
    position: absolute;
    top: 50%;
    right: 0;
    width: 24rem;
    height: 24rem;
    background: var(--accent);
    opacity: 0.04;
    border-radius: var(--radius-full);
    filter: blur(100px);
    pointer-events: none;
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  @keyframes fadeRight {
    from {
      opacity: 0;
      transform: translateX(20px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
  @keyframes fadeScale {
    from {
      opacity: 0;
      transform: scale(0.8);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }
</style>
