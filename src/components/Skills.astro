<section id="skills">
    <div class="section-inner">
        <div class="section-label">// 03. CAPABILITIES</div>

        <div class="code-card reveal">
            <div class="code-tab">
                <span class="code-filename">skills.json</span>
            </div>
            <div class="code-body">
                <ol class="code-lines">
                    <li><span class="punct">&#123;</span></li>
                    <li>
                        &nbsp;&nbsp;<span class="key">"languages"</span><span
                            class="punct">:</span
                        >
                        [<span class="str">"Java"</span>, <span class="str"
                            >"C"</span
                        >, <span class="str">"Python"</span>, <span class="str"
                            >"Rust"</span
                        >]<span class="punct">,</span>
                    </li>
                    <li>
                        &nbsp;&nbsp;<span class="key">"frameworks"</span><span
                            class="punct">:</span
                        >
                        [<span class="str">"Flask"</span>, <span class="str"
                            >"Spring"</span
                        >]<span class="punct">,</span>
                    </li>
                    <li>
                        &nbsp;&nbsp;<span class="key">"tools"</span><span
                            class="punct">:</span
                        >
                        [<span class="str">"Docker"</span>, <span class="str"
                            >"Power BI"</span
                        >, <span class="str">"Azure"</span>, <span class="str"
                            >"Git"</span
                        >]
                    </li>
                    <li><span class="punct">&#125;</span></li>
                </ol>
            </div>
        </div>
    </div>
</section>

<style>
    .section-inner {
        max-width: 75rem;
        margin: 0 auto;
        padding: 4rem var(--space-lg);
    }

    .code-card {
        max-width: 44rem;
        margin: 0 auto;
        border: 1px solid var(--border);
        border-radius: var(--radius-lg);
        background-color: var(--bg-secondary);
        overflow: hidden;
    }

    .code-tab {
        padding: var(--space-sm) var(--space-lg);
        background-color: var(--bg-accent);
        border-bottom: 1px solid var(--border);
    }

    .code-filename {
        font-family: var(--font-mono);
        font-size: 0.75rem;
        color: var(--text-secondary);
    }

    .code-body {
        padding: var(--space-lg) var(--space-xl);
        overflow-x: auto;
    }

    .code-lines {
        list-style: none;
        counter-reset: line;
        font-family: var(--font-mono);
        font-size: 0.875rem;
        line-height: 1.9;
        white-space: nowrap;
    }

    .code-lines li {
        counter-increment: line;
        padding-left: var(--space-xl);
        position: relative;
    }
    .code-lines li::before {
        content: counter(line);
        position: absolute;
        left: 0;
        width: 1.5rem;
        text-align: right;
        color: var(--text-muted);
        opacity: 0.5;
    }

    .key {
        color: var(--accent);
    }
    .str {
        color: var(--accent-green);
    }
    .punct {
        color: var(--text-muted);
    }
</style>
